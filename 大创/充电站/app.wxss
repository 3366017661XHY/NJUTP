.container {
  padding: 20px;
}
.title {
  font-size: 24px;
  font-weight: bold;
  text-align: center;
  margin-bottom: 20px;
}
.options {
  display: flex;
  justify-content: center;
}
.option {
  padding: 10px 20px;
  border: 1px solid #ccc;
  margin: 0 10px;
  cursor: pointer;
}
.question {
  margin-bottom: 20px;
}
.results {
  margin-top: 20px;
}
.info-panel {
  /* 面板样式 */
}
.inputs input {
  /* 输入框样式 */  
}